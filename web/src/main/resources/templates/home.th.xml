<thlogic>
    <attr sel="#head-Id"
          th:replace="~{fragments/head :: headFragmentMakeUp}"/>

    <attr sel="#logout"
          th:replace="~{fragments/logout :: logoutFragment}"/>

    <!--<attr sel="#logout"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).LOGIN}__}"
          th:text="#{makeup.logout}"/>-->

    <attr sel="#makeUp-title" th:text="#{makeUp.title}"/>

    <attr sel="#services"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).SERVICES}__}"
          th:text="#{makeUp.services}"/>

    <!--<attr sel="#checkAvailability"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).CHECK_AVAILABILITY}__}"
          th:text="#{makeUp.checkAvailability}"/>-->

    <attr sel="#bookAppointment"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).BOOK_APPOINTMENT}__}"
          th:text="#{makeUp.bookAppointment}"/>

    <attr sel="#bookedAppointments"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).BOOKED_APPOINTMENTS}__}"
          th:text="#{makeUp.bookedAppointments}"/>

    <attr sel="#contact"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).CONTACT}__}"
          th:text="#{makeUp.contact}"/>

    <attr sel="#myPortfolio"
          th:href="@{__${T(com.rashmi.util.MakeUpMappings).PORTFOLIO}__}"
          th:text="#{makeUp.portfolio}"/>

    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footer}"/>
</thlogic>